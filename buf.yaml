version: v1
name: buf.build/vestedsystems/connector-sdk-php
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
  except:
    # Same exceptions as the canonical proto.
    - PACKAGE_VERSION_SUFFIX
    - SERVICE_SUFFIX
    - RPC_REQUEST_RESPONSE_UNIQUE
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
